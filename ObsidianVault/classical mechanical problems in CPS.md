* These are best modeled with [[CPS model]] using [[Continuous Dynamics in CPS|continuous dynamics]].
* Everything typically surrounds the definition of [[position]], from which most of everything else can be described.